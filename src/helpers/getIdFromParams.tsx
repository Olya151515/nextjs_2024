'use client'
import React from 'react';

const GetIdFromParams = ({searchParams}:any) => {
    console.log(searchParams);
    return (
        <div>

        </div>
    );
};

export default GetIdFromParams;